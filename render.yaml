# Render.com Blueprint Infrastructure Specification for KasirIn C# .NET 8
services:
  - type: web
    name: kasirin-web
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    region: singapore
    autoDeploy: true
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
